RELEASE v7 · 24 Aug 2026 · based on codex/hostinger-deployment @ a5b671a
=========================================================================

WHAT'S NEW IN v7 — YOUTUBE FILM + (everything from v6 below)
-------------------------------------------------------------------------
0. Graduation film now streams from YouTube (https://youtu.be/6uEbqYOZxkg)
   via a click-to-load facade (local poster, zero YouTube requests until
   the visitor presses play; youtube-nocookie.com; CSP updated; VideoObject
   schema added in EN/中文/BM). The three physical videos in public/media/
   are REMOVED from the repo, along with the prebuild media-download step —
   you can DELETE the public/media folder on the server. Hero slider's
   first slide now uses the graduation poster image (faster page load).

WHAT WAS NEW IN v6 — FULL TRILINGUAL SITE + LOCALIZED PDFs
-------------------------------------------------------------------------
1. Bahasa Melayu site at /ms — all 37 pages, transcreated (not literal)
   into 2026 Malaysian news-portal register. 中文 tree completed to a full
   English mirror (16 pages added). Every page carries en / zh-Hans / ms
   hreflang alternates; header has a 3-way language switcher; sitemap,
   footer, breadcrumbs, forms, diagnostic, CTAs all trilingual.
2. All four PDFs now exist in BM and 中文 (public/downloads/):
     panduan-pengurus-bekerja-2026.pdf          (BM guide)
     zaizhi-jingli-zhinan-2026.pdf              (中文 guide)
     future-ready-employer-funding-brief-ms/zh.pdf
     future-ready-scholarship-eligibility-ms/zh.pdf
     future-ready-decision-guide-ms/zh.pdf
   The guide email now attaches the guide in the lead's own language.
   NOTE: the English guide PDF was REGENERATED — the letter signature
   carried the retired name "Rostam Affandi Ahmad"; now "Roy Affandi".
3. BM everywhere in the lead lane: BM guide email, BM nurture emails
   (day 3/7/14), /ms/unsubscribed confirmation, BM consent copy.

ONE-TIME SETUP AFTER THIS DEPLOY
-------------------------------------------------------------------------
Run migration 009 ONCE against the MySQL database:
    database/migrations/009_add_malay_language.sql
(widens page_language / outbox / nurture language enums to include 'ms' —
 WITHOUT it, /ms lead submissions will fail to store their language)

Still applicable from v5 if not yet done: migration 008 +
UNSUBSCRIBE_TOKEN_SECRET + NURTURE_CRON_SECRET + daily nurture cron.

Verified: typecheck · oxlint · 26/26 tests · content-clarity audit
(164 files incl. every BM/中文 page) · SEO checks · style budget
(re-based for 3 page trees) · full production build (134 routes).

public/media/ is intentionally GONE in v7 — delete it on the server.
Deploy: unzip over a clean checkout, npm ci && npm run build.
